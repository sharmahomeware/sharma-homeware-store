import { categories, money, products, testimonials } from './data.js';
import { addCart, clearCart, getState, restock, setAdminTab, setState, subscribe, toggleWishlist, updateAdminOrder, updateCart } from './store.js';
import { brand, cartDrawer, footer, header, icon, mobileNav, offer, productArt, productCard, stars, toast, trustMark, whatsapp } from './components.js';

const app = document.querySelector('#app');
const categoryProduct = (id) => products.find(p => p.category === id) || products[0];
const findProduct = (id) => products.find(p => p.id === id) || products[0];
const labelFor = (id) => categories.find(c => c.id === id)?.name || 'Collection';

function route() {
  const raw = location.hash.replace(/^#\/?/, '') || 'home';
  const [page, slug] = raw.split('/');
  return { page, slug };
}

const sectionIntro = (eyebrow, title, copy, href, link) => `<div class="section-intro"><div><p class="eyebrow">${eyebrow}</p><h2>${title}</h2></div>${copy ? `<div class="intro-right"><p>${copy}</p>${href ? `<a href="${href}" class="text-link">${link || 'Explore more'} ${icon('arrow', 17)}</a>` : ''}</div>` : ''}</div>`;

function hero() {
  return `<section class="hero"><div class="hero-grid"><div class="hero-copy"><p class="eyebrow">Designed for the daily ritual</p><h1>Crafted for every<br/><em>Indian kitchen.</em></h1><p class="hero-text">Beautiful, enduring steelware that turns everyday cooking into something you want to linger over.</p><div class="hero-actions"><a class="button button--primary" href="#/category/cookware">Shop the collection ${icon('arrow', 18)}</a><a class="button button--ghost" href="#/product/aurum-tri-ply-kadai"><span class="play-mark">▶</span> See the Aurum Kadai</a></div><div class="hero-trust"><span>✦ 4.8/5 from 2,000+ homes</span><span>304 food-grade steel</span></div></div><div class="hero-stage"><div class="hero-disc disc-one"></div><div class="hero-disc disc-two"></div><div class="hero-product" data-hero-product>${productArt('kadai', true)}<span class="product-sheen"></span></div><div class="hero-callout callout-one"><b>Tri-ply core</b><span>Even heat, every time</span></div><div class="hero-callout callout-two"><b>2.2 litre</b><span>The everyday size</span></div><div class="scroll-cue"><i></i> Drag to explore</div></div></div><div class="hero-bottom"><span>Preferred by 20,000+ Indian homes</span><div class="hero-logos"><b>VOGUE</b><b>architectural<br/>digest</b><b>GQ</b><b>homes &amp; gardens</b></div></div></section>`;
}

function categoriesSection() {
  return `<section class="section categories-section"><div class="container">${sectionIntro('The collection', 'Objects with a place in your home.', 'From the first chai to the last shared meal, find pieces made to live beautifully.', '#/category/cookware', 'Shop all categories')}<div class="category-grid">${categories.map((c, i) => { const p = categoryProduct(c.id); return `<a class="category-card tone-${c.tone}" href="#/category/${c.id}" data-parallax="${i}"><span class="category-index">0${i + 1}</span><div class="category-art">${productArt(p.image)}</div><div class="category-card-bottom"><h3>${c.name}</h3><p>${c.blurb}</p><span class="round-arrow">${icon('arrow', 17)}</span></div></a>`; }).join('')}</div></div></section>`;
}

function home() {
  const featured = products.slice(0, 6);
  return `${hero()}${categoriesSection()}<section class="section featured-section"><div class="container">${sectionIntro('Considered favourites', 'Made to be reached for.', 'Our most loved pieces, chosen again and again for Indian kitchens.', '#/category/cookware', 'View all products')}<div class="product-scroller">${featured.map(productCard).join('')}</div></div></section><section class="material-section"><div class="container material-grid"><div class="material-scene"><div class="steel-ribbon"></div><div class="material-object">${productArt('casserole', true)}</div><span class="material-caption"><i></i> Mirror-finished steel</span></div><div class="material-copy"><p class="eyebrow">The Sharma standard</p><h2>Built to outlast<br/>a lifetime of recipes.</h2><p>Every piece begins with responsibly sourced, food-grade steel. The kind that holds its sheen, its shape, and its place in the family for years to come.</p><div class="material-points"><span><b>304</b><small>Food-grade steel</small></span><span><b>7 yr</b><small>Structural warranty</small></span><span><b>100%</b><small>Made for Indian cooking</small></span></div><a href="#/category/steel-utensils" class="text-link">Discover our materials ${icon('arrow', 17)}</a></div></div></section><section class="section trust-section"><div class="container"><div class="trust-header"><p class="eyebrow">The Sharma promise</p><h2>Beauty you can depend on.</h2></div><div class="trust-grid">${trustMark('shield', 'Made with intention', '100% food-grade materials, always.')} ${trustMark('return', '7-day easy returns', 'A little more time to make it yours.')} ${trustMark('truck', 'Pan-India delivery', 'At your door, wherever home is.')} <div class="trust-mark"><span class="trust-icon">₹</span><div><b>COD available</b><p>Pay the way that feels right to you.</p></div></div></div></div></section><section class="testimonial-section"><div class="container testimonial-layout"><div class="testimonial-title"><p class="eyebrow">From homes like yours</p><h2>Made for the<br/><em>everyday story.</em></h2><div class="review-arrows"><button data-review="prev">←</button><button data-review="next">→</button></div></div><div class="review-rail" id="review-rail">${testimonials.map((t, i) => `<article class="review-card ${i === 0 ? 'is-current' : ''}" data-review-card="${i}"><div class="review-stars">★★★★★</div><blockquote>“${t.quote}”</blockquote><div class="review-person"><span>${t.initials}</span><p><b>${t.name}</b><small>${t.city}</small></p></div></article>`).join('')}</div></div></section><section class="newsletter"><div class="newsletter-orb"></div><div class="container newsletter-grid"><div><p class="eyebrow">The home edit</p><h2>Good things for the<br/>heart of your home.</h2></div><div><p>Join our occasional notes on new pieces, old recipes, and a thoughtfully ordered kitchen.</p><form class="email-form" data-form="newsletter"><input type="email" placeholder="Your email address" required aria-label="Your email address"/><button aria-label="Subscribe">${icon('arrow', 20)}</button></form><small>By subscribing, you agree to receive our occasional notes.</small></div></div></section>`;
}

function categoryPage(categoryId) {
  const cat = categories.find(c => c.id === categoryId) || categories[0];
  const collection = products.filter(p => p.category === cat.id);
  const shown = collection.length ? collection : products.slice(0, 6);
  return `<main class="listing-page"><div class="listing-hero"><div class="container"><div><p class="eyebrow">Sharma Homeware / ${cat.name}</p><h1>${cat.name}<span>.</span></h1><p>${cat.blurb} Designed to be used, admired, and passed on.</p></div><div class="listing-hero-art tone-${cat.tone}">${productArt(categoryProduct(cat.id).image, true)}</div></div></div><div class="container listing-body"><aside class="filters"><div class="filter-head"><b>Filters</b><button data-action="clear-filters">Clear all</button></div><details open><summary>Material <span>+</span></summary><label><input type="checkbox" checked/> Stainless steel</label><label><input type="checkbox"/> Tri-ply steel</label><label><input type="checkbox"/> Food-grade steel</label></details><details open><summary>Price <span>+</span></summary><label><input type="checkbox"/> Under ₹1,500</label><label><input type="checkbox"/> ₹1,500 – ₹3,000</label><label><input type="checkbox"/> Above ₹3,000</label></details><details><summary>Capacity / set size <span>+</span></summary><label><input type="checkbox"/> Everyday</label><label><input type="checkbox"/> Family size</label></details></aside><div class="listing-content"><div class="listing-tools"><p><b>${shown.length} pieces</b> to make everyday beautiful</p><div><button class="filter-mobile" data-action="filter-mobile">${icon('tune', 17)} Filter</button><label class="sort-select">Sort <select data-sort><option value="popular">Most loved</option><option value="low">Price: low to high</option><option value="high">Price: high to low</option><option value="new">Newest</option></select></label></div></div><div class="listing-grid" id="listing-grid">${shown.map(productCard).join('')}</div></div></div></main>`;
}

function productPage(productId) {
  const p = findProduct(productId); const related = products.filter(x => x.category === p.category && x.id !== p.id).concat(products.filter(x => x.id !== p.id)).slice(0, 4);
  return `<main class="pdp"><div class="container breadcrumb"><a href="#/home">Home</a><span>/</span><a href="#/category/${p.category}">${labelFor(p.category)}</a><span>/</span><b>${p.name}</b></div><div class="container product-layout"><section class="product-gallery"><div class="gallery-thumbs"><button class="is-selected">01</button><button>02</button><button>03</button></div><div class="product-main-view tone-silver"><span class="gallery-badge">${p.badge || 'Sharma standard'}</span>${productArt(p.image, true)}<span class="drag-360">↻ Drag for 360°</span></div></section><section class="product-details"><p class="eyebrow">${labelFor(p.category)}</p><h1>${p.name}</h1><div class="pdp-rating">${stars(p.rating)} <a href="#reviews">${p.reviews} reviews</a></div><div class="pdp-price"><strong>${money(p.price)}</strong>${p.oldPrice ? `<del>${money(p.oldPrice)}</del><span>Save ${money(p.oldPrice - p.price)}</span>` : ''}</div><p class="pdp-copy">${p.description}</p><div class="pdp-choice"><div><b>Finish</b><span id="selected-finish">${p.colors[0]}</span></div><div class="swatches">${p.colors.map((color, i) => `<button class="swatch ${i === 0 ? 'is-selected' : ''} ${color.toLowerCase()}" data-choice="finish" data-value="${color}" aria-label="${color}"></button>`).join('')}</div></div><div class="pdp-choice"><div><b>Size</b><span id="selected-size">${p.sizes[0]}</span></div><div class="size-options">${p.sizes.map((size, i) => `<button class="${i === 0 ? 'is-selected' : ''}" data-choice="size" data-value="${size}">${size}</button>`).join('')}</div></div><div class="pdp-buy"><div class="quantity quantity--large"><button data-product-qty="-1">−</button><span id="product-qty">1</span><button data-product-qty="1">+</button></div><button class="button button--primary add-product" data-add="${p.id}">Add to bag <span>${money(p.price)}</span></button><button class="wish-large ${getState().wishlist.includes(p.id) ? 'is-active' : ''}" data-wishlist="${p.id}" aria-label="Add to wishlist">${icon('heart')}</button></div><div class="pdp-assurance"><span>${icon('shield', 19)} 100% Food-grade steel</span><span>${icon('return', 19)} 7-day easy returns</span><span>${icon('truck', 19)} Pan-India delivery</span></div><div class="pdp-accordions"><details open><summary>Description <span>+</span></summary><p>Thoughtfully balanced for daily use, with a reassuring weight and a lustrous, easy-care finish.</p></details><details><summary>Specifications <span>+</span></summary><p>Material: ${p.material}<br/>Capacity: ${p.capacity}<br/>Dishwasher safe: Yes</p></details><details id="reviews"><summary>Reviews (${p.reviews}) <span>+</span></summary><p>Rated ${p.rating} by verified Sharma Homeware customers.</p></details></div></section></div><section class="section related-section"><div class="container">${sectionIntro('Complete the ritual', 'You may also like.', null, `#/category/${p.category}`, 'View all')}<div class="product-scroller">${related.map(productCard).join('')}</div></div></section></main>`;
}

function wishlistPage() {
  const wished = getState().wishlist.map(findProduct).filter(p => getState().wishlist.includes(p.id));
  return `<main class="simple-page"><div class="container page-heading"><p class="eyebrow">Your saved objects</p><h1>Wishlist<span>.</span></h1><p>${wished.length ? 'Keep an eye on the pieces that feel like home.' : 'A place for pieces you will come back to.'}</p></div><div class="container">${wished.length ? `<div class="listing-grid wishlist-grid">${wished.map(productCard).join('')}</div>` : `<div class="full-empty"><span>♡</span><h2>Nothing saved yet.</h2><p>Beautiful essentials have a way of finding their place.</p><a href="#/category/cookware" class="button button--primary">Explore the collection ${icon('arrow', 18)}</a></div>`}</div></main>`;
}

function checkoutPage() {
  const lines = getState().cart.map(line => ({ ...findProduct(line.id), qty: line.qty })).filter(p => getState().cart.find(x => x.id === p.id));
  const subtotal = lines.reduce((n, x) => n + x.price * x.qty, 0); const delivery = subtotal > 999 ? 0 : 99;
  return `<main class="checkout-page"><div class="checkout-head"><div class="container">${brand()}<p>Secure checkout</p></div></div>${lines.length ? `<div class="container checkout-grid"><form class="checkout-form" data-form="checkout"><section><h2>Delivery details</h2><div class="field-grid"><label>First name<input required placeholder="Aarav"/></label><label>Last name<input required placeholder="Sharma"/></label></div><label>Email address<input type="email" required placeholder="aarav@email.com"/></label><label>Mobile number<input required placeholder="+91 98765 43210"/></label><label>Delivery address<input required placeholder="House no., street, area"/></label><div class="field-grid"><label>City<input required placeholder="Mumbai"/></label><label>PIN code<input required placeholder="400001"/></label></div></section><section><h2>Delivery</h2><label class="radio-box is-selected"><input type="radio" checked name="delivery"/> <span><b>Standard delivery</b><small>Arrives in 3–5 business days</small></span><strong>${delivery ? money(delivery) : 'Free'}</strong></label></section><section><h2>Payment</h2><label class="radio-box is-selected"><input type="radio" checked name="payment"/> <span><b>Cash on delivery</b><small>Pay when your order arrives</small></span></label><label class="radio-box"><input type="radio" name="payment"/> <span><b>Online payment</b><small>UPI, cards, netbanking · Razorpay-ready</small></span></label></section><button class="button button--primary checkout-button" type="submit">Place order · ${money(subtotal + delivery)} ${icon('arrow', 18)}</button></form><aside class="order-summary"><h2>Your order</h2>${lines.map(p => `<div class="summary-line"><div>${productArt(p.image)}</div><span>${p.qty}</span><p>${p.name}<small>${p.capacity}</small></p><b>${money(p.price * p.qty)}</b></div>`).join('')}<div class="summary-totals"><p><span>Subtotal</span><b>${money(subtotal)}</b></p><p><span>Delivery</span><b>${delivery ? money(delivery) : 'Free'}</b></p><p class="total"><span>Total</span><b>${money(subtotal + delivery)}</b></p></div><small class="secure-note">${icon('shield', 15)} Payments are secure and encrypted.</small></aside></div>` : `<div class="container full-empty"><span>SH</span><h2>Your bag is empty.</h2><p>Add a few pieces before heading to checkout.</p><a href="#/category/cookware" class="button button--primary">Shop cookware</a></div>`}</main>`;
}

function accountPage() {
  return `<main class="account-page"><div class="container account-wrap"><aside class="account-side"><div class="account-avatar">AS</div><h2>Aarav Sharma</h2><p>aarav.sharma@email.com</p><nav><a class="is-selected" href="#/account">Overview</a><a href="#/tracking">Orders</a><a href="#/wishlist">Wishlist</a><a href="#/account">Saved addresses</a><button data-action="logout">Sign out</button></nav></aside><section class="account-content"><p class="eyebrow">My account</p><h1>Good to see you.</h1><div class="account-cards"><article><p>Recent order</p><h3>SH-81472</h3><span class="status-pill">Out for delivery</span><a href="#/tracking">Track order ${icon('arrow', 16)}</a></article><article><p>Saved pieces</p><h3>${getState().wishlist.length || '0'} objects</h3><span>Your next favourite awaits.</span><a href="#/wishlist">View wishlist ${icon('arrow', 16)}</a></article></div><div class="account-section"><div><h2>Saved addresses</h2><button class="text-button">Add new</button></div><article class="address-card"><b>Home <span>Default</span></b><p>Aarav Sharma<br/>A-12, Palm Grove Apartments<br/>Bandra West, Mumbai — 400050<br/>+91 98765 43210</p><div><button class="text-button">Edit</button><button class="text-button">Remove</button></div></article></div></section></div></main>`;
}

function trackingPage() {
  return `<main class="tracking-page"><section class="tracking-hero"><div class="container"><p class="eyebrow">Order tracking</p><h1>Know where your<br/><em>good things are.</em></h1><p>Enter your order details to see its journey home.</p><form class="track-form" data-form="track"><input required placeholder="Order ID (e.g. SH-81472)"/><input required placeholder="Email or mobile"/><button class="button button--primary" type="submit">Track order ${icon('arrow', 18)}</button></form></div></section><section class="container order-tracker" id="order-tracker"><div class="tracker-head"><div><p>Order SH-81472</p><h2>On its way to Mumbai</h2></div><span class="status-pill">Out for delivery</span></div><div class="tracker-items"><div>${productArt('kadai')}</div><p><b>Aurum Tri-Ply Kadai</b><small>24 cm · Steel</small></p><span>× 1</span></div><div class="timeline"><div class="timeline-line"><i></i></div>${[['✓','Placed','14 Jul, 10:32 AM'],['✓','Packed','14 Jul, 2:15 PM'],['✓','Shipped','15 Jul, 9:00 AM'],['4','Out for delivery','Today, 8:20 AM'],['5','Delivered','Expected today']].map((item, i) => `<div class="timeline-item ${i < 4 ? 'is-done' : ''}"><span>${item[0]}</span><p><b>${item[1]}</b><small>${item[2]}</small></p></div>`).join('')}</div><div class="tracker-footer">Estimated delivery <b>Today by 9 PM</b><button class="text-button">Need help?</button></div></section></main>`;
}

function adminPage() {
  const state = getState();
  const inventoryProducts = products.slice(0, 5);
  const orderRows = [
    { id: 'SH-81472', customer: 'Aarav Sharma', items: 'Aurum Tri-Ply Kadai', total: 2499, time: '10 min ago' },
    { id: 'SH-81460', customer: 'Meera Shah', items: 'Heritage Thali Set', total: 3299, time: '48 min ago' },
    { id: 'SH-81458', customer: 'Rohan Kapoor', items: 'Saffron Vacuum Flask', total: 1199, time: '1 hr ago' }
  ];
  const lowStock = inventoryProducts.filter(p => (state.inventory[p.id] || 0) < 10).length;
  return `<main class="admin-shell"><aside class="admin-sidebar"><a class="admin-brand" href="#/admin"><span>SH</span><b>Sharma <em>Admin</em></b></a><p class="admin-side-label">Workspace</p><nav class="admin-nav"><button class="${state.adminTab === 'overview' ? 'is-active' : ''}" data-admin-tab="overview"><i>◫</i> Overview</button><button class="${state.adminTab === 'orders' ? 'is-active' : ''}" data-admin-tab="orders"><i>□</i> Orders <span>3</span></button><button class="${state.adminTab === 'products' ? 'is-active' : ''}" data-admin-tab="products"><i>◇</i> Products</button><button class="${state.adminTab === 'customers' ? 'is-active' : ''}" data-admin-tab="customers"><i>◌</i> Customers</button></nav><div class="admin-side-bottom"><p>Store status</p><b><i></i> Live</b><a href="#/home">↗ View storefront</a></div></aside><section class="admin-main"><header class="admin-topbar"><div><p>Tuesday, 14 July</p><h1>${state.adminTab[0].toUpperCase() + state.adminTab.slice(1)}</h1></div><div class="admin-profile"><button data-action="admin-add-product">+ Add product</button><span>AS</span></div></header><div class="admin-content">${state.adminTab === 'overview' ? `<section class="admin-metrics"><article><p>Today’s revenue</p><strong>${money(46820)}</strong><span class="up">↗ 14.2% <i>vs. last Tuesday</i></span></article><article><p>Orders today</p><strong>37</strong><span class="up">↗ 8.7% <i>vs. last Tuesday</i></span></article><article><p>Average order value</p><strong>${money(2265)}</strong><span class="up">↗ 3.1% <i>vs. last Tuesday</i></span></article><article><p>Low stock alerts</p><strong>${lowStock}</strong><span class="alert-dot">Needs attention</span></article></section><section class="admin-grid"><article class="admin-card sales-card"><div class="admin-card-head"><div><p>Sales performance</p><h2>₹2.18L <span>this week</span></h2></div><select aria-label="Sales period"><option>Last 7 days</option><option>Last 30 days</option></select></div><div class="sales-chart"><div class="chart-y"><span>50k</span><span>25k</span><span>0</span></div><div class="chart-bars">${[46,62,42,81,70,89,66].map((n, i) => `<span class="${i === 5 ? 'is-today' : ''}" style="--bar:${n}%"><i>${['Mon','Tue','Wed','Thu','Fri','Sat','Sun'][i]}</i></span>`).join('')}</div></div></article><article class="admin-card orders-donut"><p>Fulfilment</p><div class="donut"><b>37<small>orders</small></b></div><ul><li><i class="dot red"></i>To pack <b>12</b></li><li><i class="dot gold"></i>In transit <b>21</b></li><li><i class="dot green"></i>Delivered <b>4</b></li></ul></article></section>` : ''}<section class="admin-card admin-table-card"><div class="admin-card-head"><div><p>${state.adminTab === 'products' ? 'Inventory' : 'Latest orders'}</p><h2>${state.adminTab === 'products' ? 'Product availability' : 'Orders to keep moving'}</h2></div><button class="admin-text-action" data-admin-tab="${state.adminTab === 'products' ? 'orders' : 'products'}">${state.adminTab === 'products' ? 'View orders' : 'Manage inventory'} →</button></div>${state.adminTab === 'products' ? `<div class="admin-table"><div class="admin-row admin-heading"><span>Product</span><span>Stock</span><span>Price</span><span></span></div>${inventoryProducts.map(p => { const stock = state.inventory[p.id] || 0; return `<div class="admin-row"><div class="admin-product">${productArt(p.image)}<p><b>${p.name}</b><small>${p.category}</small></p></div><span class="stock ${stock < 10 ? 'is-low' : ''}">${stock} ${stock < 10 ? 'low' : 'in stock'}</span><span>${money(p.price)}</span><button data-admin-restock="${p.id}">Restock +6</button></div>`; }).join('')}</div>` : `<div class="admin-table"><div class="admin-row admin-heading"><span>Order</span><span>Customer</span><span>Status</span><span>Total</span><span></span></div>${orderRows.map(order => `<div class="admin-row"><span><b>${order.id}</b><small>${order.time}</small></span><span>${order.customer}<small>${order.items}</small></span><button class="order-status ${state.adminOrders[order.id].toLowerCase().replaceAll(' ', '-') }" data-admin-order="${order.id}">${state.adminOrders[order.id]} <i>⌄</i></button><strong>${money(order.total)}</strong><button class="row-action" data-admin-next="${order.id}">Advance →</button></div>`).join('')}</div>`}</section><section class="admin-quick"><article><span>◒</span><div><p>Need to update a customer?</p><b>Send shipment updates in one place.</b></div><button data-action="admin-notify">Notify customers</button></article><article><span>✦</span><div><p>Monsoon campaign</p><b>Offer banner is currently live.</b></div><button data-action="admin-offer">Edit offer</button></article></section></div></section></main>`;
}

function searchOverlay() {
  if (!getState().searchOpen) return '';
  return `<div class="search-overlay"><button class="search-scrim" data-action="close-search" aria-label="Close search"></button><section class="search-panel"><div><p class="eyebrow">Find your next essential</p><button data-action="close-search" aria-label="Close search">${icon('close')}</button></div><label class="search-input">${icon('search', 22)}<input autofocus id="search-input" placeholder="Search cookware, dinner sets, flasks..."/></label><div class="search-results" id="search-results"><p>Popular searches</p><div class="search-tags"><button data-search="kadai">Kadai</button><button data-search="Dinner set">Dinner set</button><button data-search="Flask">Flask</button><button data-search="Storage">Storage</button></div><div class="search-popular">${products.slice(0, 3).map(p => `<a href="#/product/${p.id}">${productArt(p.image)}<span><b>${p.name}</b><small>${money(p.price)}</small></span>${icon('arrow', 17)}</a>`).join('')}</div></div></section></div>`;
}

function pageMarkup() {
  const { page, slug } = route();
  if (page === 'admin') return `${adminPage()}${toast()}`;
  if (page === 'checkout') return `${checkoutPage()}${toast()}`;
  const current = page === 'category' ? categoryPage(slug) : page === 'product' ? productPage(slug) : page === 'wishlist' ? wishlistPage() : page === 'checkout' ? checkoutPage() : page === 'account' ? accountPage() : page === 'tracking' ? trackingPage() : home();
  return `${header()}${offer()}${current}${footer()}${cartDrawer()}${searchOverlay()}${mobileNav() + whatsapp()}${toast()}`;
}

function render() { app.innerHTML = pageMarkup(); setTimeout(enhance, 0); }

function showToast(message) { const node = document.querySelector('#toast'); if (!node) return; node.textContent = message; node.classList.add('is-visible'); clearTimeout(window.toastTimer); window.toastTimer = setTimeout(() => node.classList.remove('is-visible'), 2800); }

function enhance() {
  document.querySelectorAll('[data-tilt]').forEach(card => {
    card.addEventListener('pointermove', (e) => { if (matchMedia('(hover: none)').matches) return; const rect = card.getBoundingClientRect(); const x = (e.clientX - rect.left) / rect.width - .5; const y = (e.clientY - rect.top) / rect.height - .5; card.style.transform = `perspective(1000px) rotateY(${x * 4}deg) rotateX(${y * -4}deg) translateY(-6px)`; });
    card.addEventListener('pointerleave', () => card.style.transform = '');
  });
  const heroProduct = document.querySelector('[data-hero-product]');
  if (heroProduct) { let rotation = 0, dragging = false, startX = 0; const update = () => heroProduct.style.setProperty('--drag-rotate', `${rotation}deg`); heroProduct.addEventListener('pointerdown', e => { dragging = true; startX = e.clientX; heroProduct.setPointerCapture(e.pointerId); }); heroProduct.addEventListener('pointermove', e => { if (!dragging) return; rotation += (e.clientX - startX) * .5; startX = e.clientX; update(); }); heroProduct.addEventListener('pointerup', () => dragging = false); }
  const headerNode = document.querySelector('.site-header');
  if (headerNode) window.onscroll = () => headerNode.classList.toggle('is-scrolled', window.scrollY > 14);
  const accountNav = document.querySelector('.account-side nav');
  if (accountNav) accountNav.insertAdjacentHTML('beforeend', '<a href="./admin.html">Open admin window ↗</a>');
}

document.addEventListener('click', (event) => {
  const target = event.target.closest('button, [data-wishlist], [data-add], [data-qty], [data-remove], [data-choice], [data-review]'); if (!target) return;
  if (target.dataset.add) { const qty = Number(document.querySelector('#product-qty')?.textContent || 1); addCart(target.dataset.add, qty); showToast('Added to your bag.'); return; }
  if (target.dataset.wishlist) { const id = target.dataset.wishlist; const existing = getState().wishlist.includes(id); toggleWishlist(id); showToast(existing ? 'Removed from your wishlist.' : 'Saved to your wishlist.'); return; }
  if (target.dataset.qty) { const line = getState().cart.find(x => x.id === target.dataset.qty); updateCart(target.dataset.qty, (line?.qty || 0) + Number(target.dataset.delta)); return; }
  if (target.dataset.remove) { updateCart(target.dataset.remove, 0); showToast('Removed from your bag.'); return; }
  if (target.dataset.productQty) { const qtyNode = document.querySelector('#product-qty'); const next = Math.max(1, Number(qtyNode.textContent) + Number(target.dataset.productQty)); qtyNode.textContent = next; return; }
  if (target.dataset.choice) { const selector = target.dataset.choice; document.querySelectorAll(`[data-choice="${selector}"]`).forEach(x => x.classList.remove('is-selected')); target.classList.add('is-selected'); document.querySelector(`#selected-${selector}`).textContent = target.dataset.value; return; }
  if (target.dataset.action === 'cart') { setState({ cartOpen: true }); return; }
  if (target.dataset.action === 'close-cart') { setState({ cartOpen: false }); return; }
  if (target.dataset.action === 'search') { setState({ searchOpen: true }); return; }
  if (target.dataset.action === 'close-search') { setState({ searchOpen: false }); return; }
  if (target.dataset.action === 'dismiss-offer') { setState({ offerVisible: false }); return; }
  if (target.dataset.action === 'checkout') { setState({ cartOpen: false }); return; }
  if (target.dataset.action === 'menu') { showToast('Browse a collection from the navigation above.'); return; }
  if (target.dataset.action === 'filter-mobile') { showToast('Filters are shown on larger screens.'); return; }
  if (target.dataset.action === 'clear-filters') { document.querySelectorAll('.filters input').forEach((x, i) => x.checked = i === 0); showToast('Filters cleared.'); return; }
  if (target.dataset.review) { const cards = [...document.querySelectorAll('[data-review-card]')]; let index = cards.findIndex(x => x.classList.contains('is-current')); index = target.dataset.review === 'next' ? (index + 1) % cards.length : (index - 1 + cards.length) % cards.length; cards.forEach((x, i) => x.classList.toggle('is-current', i === index)); return; }
  if (target.dataset.search) { const input = document.querySelector('#search-input'); input.value = target.dataset.search; input.dispatchEvent(new Event('input')); return; }
  if (target.dataset.adminTab) { setAdminTab(target.dataset.adminTab); return; }
  if (target.dataset.adminRestock) { restock(target.dataset.adminRestock); showToast('Stock increased by 6 units.'); return; }
  if (target.dataset.adminNext || target.dataset.adminOrder) { const id = target.dataset.adminNext || target.dataset.adminOrder; const statuses = ['Payment review', 'Packed', 'Out for delivery', 'Delivered']; const current = getState().adminOrders[id] || statuses[0]; const next = statuses[(statuses.indexOf(current) + 1) % statuses.length]; updateAdminOrder(id, next); showToast(`${id} marked “${next}”.`); return; }
  if (target.dataset.action === 'admin-add-product') { showToast('Product editor is ready to connect to your catalog API.'); return; }
  if (target.dataset.action === 'admin-notify') { showToast('Shipment update draft prepared for 21 customers.'); return; }
  if (target.dataset.action === 'admin-offer') { showToast('Offer editor is ready to connect to your campaign settings.'); return; }
  if (target.dataset.action === 'logout') { showToast('You have been signed out.'); return; }
});

document.addEventListener('input', (event) => {
  if (event.target.id !== 'search-input') return; const q = event.target.value.toLowerCase().trim(); const found = products.filter(p => `${p.name} ${p.category}`.toLowerCase().includes(q)).slice(0, 5); const root = document.querySelector('#search-results'); if (!q) return;
  root.innerHTML = found.length ? found.map(p => `<a href="#/product/${p.id}">${productArt(p.image)}<span><b>${p.name}</b><small>${money(p.price)} · ${labelFor(p.category)}</small></span>${icon('arrow', 17)}</a>`).join('') : '<p class="no-results">No objects found. Try “kadai” or “flask”.</p>';
});

document.addEventListener('change', (event) => {
  if (!event.target.matches('[data-sort]')) return; const grid = document.querySelector('#listing-grid'); let cards = products.filter(p => p.category === route().slug); if (!cards.length) cards = products.slice(0, 6); if (event.target.value === 'low') cards.sort((a,b) => a.price-b.price); if (event.target.value === 'high') cards.sort((a,b) => b.price-a.price); if (event.target.value === 'new') cards.sort((a,b) => Number(Boolean(b.badge))-Number(Boolean(a.badge))); grid.innerHTML = cards.map(productCard).join(''); enhance();
});

document.addEventListener('submit', (event) => {
  if (!event.target.matches('form')) return; event.preventDefault();
  if (event.target.dataset.form === 'checkout') { clearCart(); location.hash = '#/tracking'; showToast('Order placed — thank you.'); }
  if (event.target.dataset.form === 'newsletter') { event.target.reset(); showToast('Welcome to the home edit.'); }
  if (event.target.dataset.form === 'track') { document.querySelector('#order-tracker')?.scrollIntoView({ behavior: 'smooth' }); showToast('Order SH-81472 is on its way.'); }
});

window.addEventListener('hashchange', () => { window.scrollTo({ top: 0, behavior: 'auto' }); render(); });
window.addEventListener('sharma:platform-updated', render);
subscribe(render);
if (!location.hash) location.hash = '#/home'; else render();
